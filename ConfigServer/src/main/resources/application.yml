server:
  port: 8086

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/sagarGlobalLogic2023/HotelRating-Configuration-Microservice/
          clone-on-start: true
